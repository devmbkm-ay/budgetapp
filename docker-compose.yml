services:
  api:
    build:
      context: .
      target: api
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - PORT=3001
    depends_on:
      - db
    restart: always

  web:
    build:
      context: .
      target: web
    ports:
      - "3000:3000"
    environment:
      - API_URL=http://api:3001
      - NEXT_PUBLIC_API_URL=http://your-domain.com/api # Replace on real deploy
      - AUTH_SECRET=${AUTH_SECRET}
    depends_on:
      - api
    restart: always

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=budget_db
    volumes:
      - budget_db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: always

volumes:
  budget_db_data:
